angular.module('transac.top-bar').component('topBar', {
  bindings: {
  },
  templateUrl: 'common/top-bar',
  controller: ()->
    ctrl = this;

    ctrl.$onInit = () ->

    ctrl.menuItemOnClick = ->

    return
})
